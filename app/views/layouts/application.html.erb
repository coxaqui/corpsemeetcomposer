<!DOCTYPE html>
<html>
<head>
  <title>Composer, Meet Corpse</title>
  <%= stylesheet_link_tag "web-app-theme/base", "web-app-theme/themes/drastic-dark/style", "web-app-theme/override", :cache => true %>
  <%= javascript_include_tag :defaults, :cache => true %>
  <%= csrf_meta_tag %>
</head>
<body>
  <div id="container">
    <div id="header">
      <h1><a href="/">Composer, Meet Corpse</a></h1>
      <div id="user-navigation">
        <ul class="wat-cf">
			<li><%= link_to 'Home', viewerLinkeage_path( 'home' ) %> </li>
			<li><%= link_to 'Metal', viewerLinkeage_path( 'metal' ) %> </li>
			<li><%= link_to 'About Us', viewerLinkeage_path( 'about' ) %> </li>
			<li><%= link_to 'Contact Us', viewerLinkeage_path( 'contact' ) %> </li>
			<li><%= link_to 'More', viewerLinkeage_path( 'more' ) %> </li>
			<%if user_signed_in?%>
			<%end%>
			<% if user_signed_in? %>
				<li class="admin"><%= link_to 'Admin', viewerLinkeage_path( 'pages' ) %></li>
				<li></li>						
				<li class="logout"><%= link_to " Sign out", destroy_user_session_path %></li>
			<% else %>
				<li class="login"> <%= link_to "Sign in", new_user_session_path %></li>
			<% end %>
        </ul>
      </div>
      <div id="main-navigation">
        <ul class="wat-cf"></ul>
      </div>
    </div>    
    <div id="wrapper" class="wat-cf">
      <div class="flash">
        <% flash.each do |type, message| -%>
          <div class="message <%= type %>">
            <p><%= message %></p>
          </div>
        <% end -%>
      </div>
      <div id="main">
        <%= yield %>
        <div id="footer">
          <div class="block">
            <p>Copyright &copy; <%= Time.now.year %> Composer, Meet Corpse.</p>
          </div>      
        </div>
      </div>
      <div id="sidebar">
        <%= yield :sidebar %>
      </div>
    </div>    
  </div>
</body>
</html>
